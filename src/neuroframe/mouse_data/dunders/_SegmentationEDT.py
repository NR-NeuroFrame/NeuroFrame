class Dunders:
    def __repr__(self):
        return f'SegmentationEDT(path="{self.path}")'
    def __str__(self):
        return f"SegmentationEDT stored in {self.folder} and with filename {self.filename}"
